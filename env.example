# =============================================================================
# COMM360 VIDEO CALLING APPLICATION - DOCKER INTEGRATION ENVIRONMENT
# =============================================================================
# This file contains all environment variables needed for Docker integration
# with the main company website. Copy to .env and configure for your setup.

# =============================================================================
# FRONTEND SERVICE (webrtc-client) - Port 3000
# =============================================================================
# Main website integration URLs
VITE_API_URL=https://yourdomain.com/comm360/api
VITE_SFU_URL=https://yourdomain.com/sfu

# TURN Server Configuration
VITE_TURN_SERVER_URL=turn:yourdomain.com:3478
VITE_TURN_USERNAME=webrtc
VITE_TURN_PASSWORD=webrtc

# Frontend Configuration
VITE_GOOGLE_CLIENT_ID=your-google-client-id
VITE_ENABLE_RECORDING=true
VITE_ENABLE_BOT_FEATURES=true
VITE_PORT=3000

# =============================================================================
# BACKEND SERVICE (webrtc-signaling-server) - Port 5000
# =============================================================================
# Server Configuration
PORT=5000
SERVER_PORT=5000
NODE_ENV=production

# Integration URLs (for main website)
BACKEND_URL=https://yourdomain.com/comm360/api
CORS_ORIGIN=https://yourdomain.com

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/comm360?retryWrites=true&w=majority

# Authentication
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# File Upload Configuration
MAX_FILE_SIZE=10485760

# =============================================================================
# SFU SERVICE (Media Server) - Port 8181 + UDP 30000-30010
# =============================================================================
# SFU Server Configuration
SFU_PORT=8181
SERVER_PORT=8181

# Network Configuration (CRITICAL for WebRTC)
PUBLIC_IP=your-server-public-ip
EXTERNAL_IP=your-server-public-ip

# TURN Integration
TURN_URL=turn:yourdomain.com:3478
TURN_USER=webrtc
TURN_PASS=webrtc

# =============================================================================
# TURN SERVER (Coturn) - Port 3478 TCP/UDP
# =============================================================================
# TURN Server Authentication
TURN_SECRET=your-turn-server-secret-key
TURN_USER=webrtc
TURN_PASS=webrtc

# Network Configuration
EXTERNAL_IP=your-server-public-ip

# =============================================================================
# DOCKER INTEGRATION NOTES
# =============================================================================
# 1. Replace 'yourdomain.com' with your actual domain
# 2. Set PUBLIC_IP/EXTERNAL_IP to your server's public IP
# 3. Configure MongoDB Atlas connection string
# 4. Generate secure JWT_SECRET (minimum 32 characters)
# 5. Set up Google OAuth credentials if using Google login
# 6. Ensure CORS_ORIGIN matches your main website domain
# 7. TURN server is critical for WebRTC across NAT/firewalls 